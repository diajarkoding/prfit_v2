import 'package:flutter/material.dart';

class BackList extends StatelessWidget {
  const BackList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('BackList'),
      ),
    );
  }
}
