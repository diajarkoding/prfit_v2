import 'package:flutter/material.dart';

class ShouldersList extends StatelessWidget {
  const ShouldersList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ShouldersList'),
      ),
    );
  }
}
