import 'package:flutter/material.dart';

class ForearmsList extends StatelessWidget {
  const ForearmsList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('ForearmsList'),
      ),
    );
  }
}
