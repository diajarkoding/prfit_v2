import 'package:flutter/material.dart';

class Bicepslist extends StatelessWidget {
  const Bicepslist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Bicepslist'),
      ),
    );
  }
}
